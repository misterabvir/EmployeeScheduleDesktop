<Window xmlns:ShiftEditor="clr-namespace:ShiftsUI.Editors.ShiftEditor"  x:Class="ShiftsUI.Dialogs.ShiftEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShiftsUI.Dialogs"
        mc:Ignorable="d"
        Title="редактирование смен" Height="630" Width="470" Style="{StaticResource windStyle}" WindowStyle="ToolWindow" ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <ShiftEditor:ShiftEditorControl Grid.ColumnSpan="2" Grid.Column="0" Grid.Row="0"/>
        <Button Content="{StaticResource BtnClose}" Margin="5" Grid.Column="2" Grid.Row="1" Click="ButtonClose_Click"  Style="{StaticResource  menuBtn}"/>
    </Grid>
</Window>
